#include <stdio.h>

int main()
{
    int a,b,c;

    printf("Enter an integer: ");
    scanf("%d,%d", &a,&b);
     c=a-b;
     printf("%d",c);
    if(number % 2 == 0)
    {
   printf("%d is even.",c) ;
    }
    else
    {
   printf("%d is odd.",c);
    }
    return 0;
}
