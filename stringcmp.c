#include<stdio.h>
#include<conio.h>
void main()
{
char str1[20,str2[20];
int compare;
printf("enter the first string:");
scanf("%s",&str1);
printf("enter  the second string:");
scanf("%s",&str2);
compare=stringtcompare(str1,str2);
if(compare==1)
{
printf("both are equal:");
}
else
{
printf("not eqal"):
return 0;
}

