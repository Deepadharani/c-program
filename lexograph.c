#include<stdio.h>
#include<string.h>
int main()
{
int i,j,k;
char str[100],tem[100];
printf("enter the string");
scanf("%c[^\n]s",str);
for(i=oi<str[i]!='\0';i++)
{
for(j=i+1;dtr[j]!='\0';j++)
{
if(dtr[i]>str[j])
{
temp[k]=str[i];
str[i]=str[j];
str[j]=temp[k];
}
return 0;
}
