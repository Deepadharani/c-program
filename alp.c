#include<stdio.h>
int main()
{
 char c;
 printf("enter a character:");
 scanf("%c",&c);
 if((c>='a' && c<='z') || (c>='A' && c<='z'))
 {
 printf("%c is not an alphebet.',c);
 else
 {
 printf(%c is not an alphebet.",c);
 return 0;
 }
